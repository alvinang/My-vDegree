<% @course.each_with_index do |item, index| %>
<tr>
  <td><%= index + 1%></td>
  <td><%= item.name %></td>
  <td><%= item.url %></td>
  <td><%= item.start_date %> - <%= item.end_date %></td>
</tr>

<% end %>